---
description: 從文法講義 .md 產生每單元漫畫產圖 prompt（*-picture.md）
globs: Content/grammar/**/*.md
alwaysApply: false
---

# 文法講義 → 每單元產圖 Prompt

當使用者在對話中**附上或 @ 某個文法講義 `.md`**，並要求產生「產圖 prompt」「picture 指令」「`-picture.md`」「漫畫學文法」等時，依下列流程產出檔案。

## 必讀規格（專案內）

優先遵循：`Content/grammar/PICTURE-PROMPT-FROM-LECTURE.md`（單元切割、三格／四格、輸出檔名、固定句式）。

## 輸出檔名與位置

- 與講義**同一資料夾**：`{講義檔名}-picture.md`（例：`WHQA.md` → `WHQA-picture.md`）。

## 單元邊界

- 每個以 `## ` 開頭的章節 = **一則**產圖指令（檔首單一 `#` 標題不算一單元）。
- 若使用者明確要求合併章節（例：9 與 10），改為 `## 單元 9、10 — …` 一則，`內容細項` 註明講義第幾單元；仍以**三格為預設**。

## 每一則指令的固定格式（結構不可改）

1. 第一段：`請為我生成一張圖，要求如下：`
2. `主題與來源：參考來源內容細項，製作一張「漫畫學文法：…」。`
3. `畫面佈局要求（參考三個圖片資源）：` 底下三點**逐字**為：
   - `1.請編寫「文法博士」與「新手小白」，博士負責點出規則重點，小白負責說例句。`
   - `2. 目前對話偏多，圖片是漫畫風格，字越少越好。`
   - `3. 圖片需全英語。`
4. `內容細項：…（{講義檔名}.md 第 N 單元），請照下面敘述分成三格`（必要時改「四格」並簡述理由）。
5. 底下 `1. 2. 3.` 分格：含 `👉` 句型列與**英文例句**（例句多行較佳）；說明可中文。

## 內容來源

從該 `##` 區塊內擷取：規則句、`👉` 行、已有例句、❌／⭕ 對照；勿虛構與講義矛盾的規則。

## 參考範例檔

`Content/grammar/Grammar-Basic/WHQA+Dummy Subject/WHQA-picture.md`
